{
  "name": "dogfood-broken-project",
  "version": "1.0.0",
  "description": "Dogfooding demo project for Jewel CLI",
  "main": "dist/math.js",
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "test": "node --test dist/math.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}

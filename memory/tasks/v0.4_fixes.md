# Tasks - Jewel v0.4 Fixes & Acceptance

- [ ] Problem 1: Create `scripts/run-all-tests.js` to recursively run all tests and update `package.json` `"test"` script.
- [ ] Problem 2: Update `PlanInput` interface and `runTask` call to pass `filesNeeded`, update `MockAgentAdapter.plan` to respect the files scope, and update `MockAgentAdapter.proposePatch` to return TS/JS math implementations depending on file extension.
- [ ] Problem 3: Update `examples/dogfood-broken-project/package.json` scripts to build before testing.
- [ ] Problem 4: Bump package version to `0.4.0` in `package.json` and `package-lock.json`.
- [ ] Problem 5: Add run-level mocked tests for Gemini, Anthropic, and OpenRouter in `src/cli/commands/run.test.ts`.
- [ ] Verification: Build, run tests (asserting 69+ tests passing), run doctor, pack dry-run, and verify dogfood project e2e manually.

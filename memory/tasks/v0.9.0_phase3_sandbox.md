# Checklist: Jewel v0.9.0 Phase 3 - Isolated Sandbox Verification Runner

- [x] Configuration Schema Updates (`src/core/config.ts`)
  - [x] Add `useSandbox?: boolean`, `sandboxFallbackToHost?: boolean`, `sandboxImage?: string`, `sandboxVolumes?: Record<string, string>`, and `sandboxEnv?: Record<string, string>` to `JewelConfig`
  - [x] Add default values to `DEFAULT_CONFIG`
  - [x] Update `validateAndMergeConfig()`:
    - [x] Validate `useSandbox` is boolean
    - [x] Validate `sandboxFallbackToHost` is boolean
    - [x] Validate `sandboxImage` is string
    - [x] Validate `sandboxVolumes` is non-null plain object with string keys and string values
    - [x] Validate that all values in `sandboxVolumes` are absolute paths (start with `/`)
    - [x] Validate `sandboxEnv` is non-null plain object with string keys and string values
- [x] Configuration Unit Tests (`src/core/config.test.ts`)
  - [x] Add tests for new sandbox configuration default values
  - [x] Add tests verifying valid sandbox volumes and environments pass validation
  - [x] Add tests verifying invalid volume target paths (non-absolute) throw errors
  - [x] Add tests verifying invalid types throw validation errors
- [x] Verification Runner Updates (`src/verification/runner.ts`)
  - [x] Change import to `import * as cp from 'child_process';` and update line 93 from `execSync(...)` to `cp.execSync(...)`
  - [x] Implement `dockerUtils` namespace containing:
    - [x] `isDockerAvailable()` with `timeout: 3000` option
    - [x] `executeDocker(args, cwd, env)` calling `cp.spawnSync`
  - [x] Update `runVerification()`:
    - [x] Run `dockerUtils.isDockerAvailable()` exactly once before loop if `useSandbox` is enabled
    - [x] Implement host execution fallback when `useSandbox` is enabled but Docker is unavailable:
      - [x] If `sandboxFallbackToHost` is `true`, execute on host and prepend warning message to `stderr`
      - [x] If `sandboxFallbackToHost` is `false` (default), abort execution and return `FAIL` status with a descriptive `stderr`
    - [x] Implement Docker sandbox command execution when `useSandbox` and `dockerAvailable` are true:
      - [x] Normalize paths (`cwd`, `dist` mount, and custom volumes) to use `/` instead of `\`
      - [x] Build `dockerArgs` array
      - [x] Conditionally append `--user` for Linux/Unix hosts
      - [x] Assign `execEnv.HOME = '/tmp'` and append `-e HOME` to `dockerArgs`
      - [x] Mount Jewel's `dist` folder to `/opt/jewel/dist`
      - [x] Add process auditing env (`JEWEL_AUDIT_CONFIG` and `NODE_OPTIONS`) if `auditSpawnedProcesses` is enabled
      - [x] Add custom volume mounts with absolute host path resolution
      - [x] Add custom environment variables with host resolution support (resolving values starting with `$`)
      - [x] Append image and shell invocation `sh -c` with commands
      - [x] Run `dockerUtils.executeDocker`
      - [x] Handle signal termination and start errors cleanly (when `spawnResult.status` is null)
- [x] Verification Runner Tests (`src/verification/runner.test.ts`)
  - [x] Add unit tests for Docker unavailable fallback behaviour (with and without fallbackToHost enabled)
  - [x] Add unit tests for successful Docker command assembly and execution (mocking `executeDocker`)
  - [x] Verify environment variables mapping and process isolation in tests
  - [x] Ensure all mocks are restored after each test
- [x] Verification
  - [x] Run `npm run build`
  - [x] Run `npm test` and ensure all tests pass


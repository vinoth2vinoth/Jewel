# Task Checklist: Jewel v0.10.0 Phase 1 - Semantic Symbol Isolation Guard

- [x] **1. Extend Configuration & Contract Schemas**
  - [x] Add `allowedSymbolChanges?: string[]` to `JewelConfig` in `src/core/config.ts`.
  - [x] Add `allowedSymbolChanges: []` to `DEFAULT_CONFIG` in `src/core/config.ts`.
  - [x] Support `allowedSymbolChanges` in `validateAndMergeConfig` in `src/core/config.ts`.
  - [x] Add `allowedSymbolChanges` to `TaskContractSchema` in `src/agents/structured-schema.ts`.
  - [x] Add `allowedSymbolChanges?: string[]` to `TaskContract` in `src/core/session.ts`.
  - [x] Validate `allowedSymbolChanges` in `validateTaskContractJson` in `src/agents/json-response.ts`.

- [x] **2. Implement Symbol Matching & Merging in Diff Guard**
  - [x] Update `runDiffGuard` and `runASTDiffAnalysis` parameter list in `src/safety/diff-guard.ts`.
  - [x] Implement config-level and contract-level `allowedSymbolChanges` merging and empty string filtering inside `runDiffGuard`.
  - [x] Update `runASTDiffAnalysis` matching logic to check signature changes against allowed symbols using safe exact, substring, and token-split comparison.
  - [x] Pass `contract.allowedSymbolChanges` to `runDiffGuard` inside `src/cli/commands/run.ts`.

- [x] **3. Write Unit Tests**
  - [x] Add unit tests for config merging in `src/core/config.test.ts`.
  - [x] Add unit tests for contract schema validation in `src/agents/json-response.test.ts`.
  - [x] Add unit tests for symbol isolation guard bypass in `src/safety/diff-guard.test.ts`.

- [x] **4. Build & Test Verification**
  - [x] Compile TypeScript files via `npm run build`.
  - [x] Run test suite via `npm test` and verify all tests pass.

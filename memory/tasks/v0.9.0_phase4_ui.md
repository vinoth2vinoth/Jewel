# Task Checklist: Jewel v0.9.0 Phase 4 - Local Web UI Dashboard

- [x] **1. Refactor Verification Runner to Async**
  - [x] Refactor `dockerUtils.executeDocker` signature to return a `Promise` and accept an `onChunk` callback for streaming stdout/stderr.
  - [x] Update `dockerUtils.executeDocker` implementation in `src/verification/docker.ts` (or equivalent) to use async child process spawning and stream output.
  - [x] Refactor `runVerification` in `src/verification/runner.ts` to be `async`, sequential, wrap executions in try-catch to log failures with exit code `99` rather than crashing, and support progress callback.
  - [x] Refactor `src/verification/runner.test.ts`: make all tests `async`, update `executeDocker` mocks, and await `runVerification`.

- [x] **2. Update CLI commands to support async verification**
  - [x] Refactor `runVerify` in `src/cli/commands/verify.ts` to be `async`, await `runVerification`, and let `main()` handle process exit.
  - [x] Update `src/cli/index.ts` to support `--ui` flag, pass it to `runTask`, and await the async `runVerify` wrapper.

- [x] **3. Implement Dashboard HTML Template**
  - [x] Create `src/cli/dashboard-html.ts` exporting `DASHBOARD_HTML` string with properly escaped backticks, CSS backslashes, and template symbols.
  - [x] Verify HTML layout features modern responsive CSS styling (glassmorphism), log streaming, interactive action modals, and exit controls.

- [x] **4. Implement UI Server (`src/cli/ui-server.ts`)**
  - [x] Design the `UIServer` constructor, supporting `startPort` (defaulting to 3000) and `autoOpenBrowser` flag.
  - [x] Generate crypto session-scoped token and protect static index / API endpoints with authorization checks.
  - [x] Implement SSE `/api/events` endpoint, pushing state, maintaining registry of clients, and filtering payloads through `redactSecrets`.
  - [x] Implement `POST /api/action` endpoint, buffering requests up to 10KB, parsing body, and resolving approval or exit promises.
  - [x] Implement port hunting (ports 3000 to 3100) and OS-specific browser auto-launch.
  - [x] Implement robust `close()` method to clean up all keep-alive connections and close SSE streams.

- [x] **5. Integrate Server into `runTask` execution loop (`src/cli/commands/run.ts`)**
  - [x] Support `useUI` parameter in `runTask` signature.
  - [x] Start `uiServer` at task initiation and log access URL.
  - [x] Forward runner progress callbacks to `uiServer`.
  - [x] Redirect approval prompts (`scope-expansion`, `patch-review`, `retry-choice`) to the UI server if `useUI` is true.
  - [x] Wrap execution loop in try-catch-finally, wait for acknowledgment before CLI exit on failure/success, and guarantee server close.

- [x] **6. Add UI Server Unit Tests**
  - [x] Create `src/cli/ui-server.test.ts` to test server startup, token checks, SSE updates, port hunting, and action handling.

- [x] **7. Verification and Hardening**
  - [x] Compile TS codebase with `npm run build`.
  - [x] Run entire test suite via `npm test` and ensure all tests pass.
  - [x] Update the Graphify knowledge graph using `graphify update .`.

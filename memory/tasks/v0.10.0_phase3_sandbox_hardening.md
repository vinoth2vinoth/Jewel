# Checklist: Jewel v0.10.0 Phase 3 - Configurable Process Auditing & Sandbox Rules

- [x] Add new configuration fields to `JewelConfig` and `DEFAULT_CONFIG` in `src/core/config.ts`
- [x] Add config validations in `validateAndMergeConfig()` in `src/core/config.ts`
- [x] Update `DEFAULT_CONFIG_CONTENT` in `src/core/templates.ts` to include default values for the new properties
- [x] Update `README.md` configuration section to document the new sandbox properties
- [x] Update Docker execution argument assembly in `src/verification/runner.ts` to support `sandboxNetwork` (`--network <net>`)
- [x] Update Docker execution argument assembly in `src/verification/runner.ts` to support `sandboxReadOnlyRoot` (`:ro` mount), `sandboxWritePaths` (`:rw` mounts), symlink escape checks, and directory type validation
- [x] Add unit tests in `src/core/config.test.ts` for new configuration options validation (including negative escape and normalization tests)
- [x] Update the existing sandbox test and add new unit tests in `src/verification/runner.test.ts` to verify Docker command line arguments, symlink check, and directory creation
- [x] Verify everything compiles and all tests pass with `npm run build` and `npm test`

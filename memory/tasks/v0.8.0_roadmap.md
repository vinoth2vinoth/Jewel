# Checklist: Jewel v0.8.0 - Code Coverage, Process Auditing, Interactive Overrides, and Budget Guards

- [x] Phase 1: Test Coverage Guard
  - [x] Update config schema and defaults in `src/core/config.ts` for `"minCoverage"` and `"coverageReportPath"`
  - [x] Implement coverage report parsing and validation in `src/verification/runner.ts`
  - [x] Add `COVERAGE_THRESHOLD_VIOLATION` status to execution statuses
  - [x] Implement unit tests for coverage validation with mock reports

- [x] Phase 2: Runtime Process Auditing
  - [x] Create `src/verification/preload.ts` (process preloader)
  - [x] Integrate process auditor preloader inside `src/verification/runner.ts` via NODE_OPTIONS
  - [x] Enforce security checks on spawned processes (network, SSH keys, destructive commands)
  - [x] Implement unit tests for process auditor and verify blocked triggers

- [x] Phase 3: Interactive Override & Debug Loop
  - [x] Update `src/cli/commands/run.ts` run execution loop to check for interactive terminal context
  - [x] Prompt user on failure with options to Retry (with custom hint), Override, or Abort
  - [x] Implement and test retry hint injection into `buildPatchProposalPrompt`
  - [x] Add E2E smoke tests mocking interactive inputs

- [ ] Phase 4: Session Cost & Budget Guard
  - [ ] Add model pricing structure to `src/agents/model-capabilities.ts`
  - [ ] Track cumulative cost estimates inside `src/core/session.ts` and `src/agents/adapter.ts`
  - [ ] Exit with `BUDGET_EXCEEDED` status if costs breach maximum threshold
  - [ ] Write unit tests for cumulative cost calculation and budget limits

- [ ] Phase 5: Verification & Integration
  - [ ] Verify TypeScript compiles cleanly (`npm run build`)
  - [ ] Run entire suite of tests (`npm test`) and check that no regressions occur

# Checklist: Jewel v0.9.0 Phase 1 - Critic Test Fixes

- [x] Update `src/cli/commands/run-critic-retry.test.ts`
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop successfully retries and passes` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop hits retry limit and stops with RETRY_LIMIT_REACHED` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop stops early on same verification failure logs` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop stops on UNKNOWN verdict with NEEDS_HUMAN_REVIEW` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop detects bad generated test and returns GENERATED_TEST_SUSPECT` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop blocks modification of existing tests with EXISTING_TEST_MODIFIED` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop - interactive custom hint retry` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop - interactive override success` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `repair loop aborts immediately on budget limit breach with BUDGET_EXCEEDED and rolls back` test
- [x] Update `src/cli/commands/run.test.ts`
  - [x] Add `ReviewResult` mock handler to fetch mock in `OpenAI integration safe patch writes successfully` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `OpenAI integration blocked for parent traversal` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `OpenAI integration blocked for absolute path` test
  - [x] Add `ReviewResult` mock handler to fetch mock in `CLI provider overrides and validation` test
  - [x] Add `ReviewResult` mock handler to Gemini fetch mock in `Gemini integration safe patch and unsafe blocking` test
  - [x] Add `ReviewResult` mock handler to Anthropic fetch mock in `Anthropic integration safe patch and unsafe blocking` test
  - [x] Add `ReviewResult` mock handler to OpenRouter fetch mock in `OpenRouter integration safe patch and unsafe blocking` test
- [x] Verify
  - [x] Run `npm run build`
  - [x] Run `npm test` and verify all tests pass without hanging

{
  "version": "1.0.0",
  "description": "Curated Jewel benchmark tasks for measuring agent success rate.",
  "tasks": [
    {
      "id": "dogfood-math-scoped",
      "description": "Fix divide-by-zero with explicit file scope",
      "projectPath": "examples/dogfood-broken-project",
      "task": "fix the failing math test for division by zero",
      "files": ["src/math.ts"],
      "useMock": true,
      "preCheck": "broken-tests",
      "verifyCommand": "npm test",
      "cleanup": [".jewel", "dist"],
      "resetFixture": true
    },
    {
      "id": "dogfood-math-auto",
      "description": "Fix divide-by-zero without --files (auto-discovery)",
      "projectPath": "examples/dogfood-broken-project",
      "task": "fix the failing math divide by zero test",
      "files": [],
      "useMock": true,
      "preCheck": "broken-tests",
      "verifyCommand": "npm test",
      "cleanup": [".jewel", "dist"],
      "resetFixture": true
    },
    {
      "id": "create-node-api-mock",
      "description": "Scaffold the node-api blueprint and run one mock milestone on it",
      "projectPath": "benchmarks/tmp/create-node-api-mock",
      "scaffold": "node-api",
      "task": "add a request logging helper module",
      "files": ["src/logger.js"],
      "useMock": true,
      "verifyCommand": "npm test",
      "cleanup": ["."]
    }
  ]
}
